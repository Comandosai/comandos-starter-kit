# Правила проекта (Google Antigravity)

> Кладётся в `.agent/rules/project.md` создаваемого проекта.

Проект: {{PROJECT_NAME}} ({{PROJECT_TYPE}}, стек {{STACK}}).
Полные правила — в `AGENTS.md` в корне. Прочитай его первым.

Принципы: не ломать структуру; новый код — в правильное место; секреты только в `.env`
(в `.gitignore`); общение на русском, просто, без стектрейсов; решения принимать с владельцем.
