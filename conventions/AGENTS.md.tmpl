# AGENTS.md — правила работы для AI в этом проекте

> Этот файл создан COMANDOS Starter Kit. Любой AI-агент, открывший проект,
> читает его первым и следует правилам.

## Проект
- **Название:** {{PROJECT_NAME}}
- **Тип:** {{PROJECT_TYPE}}
- **Стек:** {{STACK}}
- **Создан:** {{CREATED}}

## Что это
{{ONE_LINER}}

## Структура (куда что класть)
- Код — в `src/`
- Документация/заметки — в `docs/`
- Тяжёлые медиа (видео, исходники) — НЕ в git (см. база знаний §17)
- Секреты — только в `.env` (он в `.gitignore`), НИКОГДА не коммить

## База знаний
{{KNOWLEDGE_LINK}}

## Правила
- Перед изменением кода — пойми существующую структуру, не ломай её.
- Новый код — в правильное место по структуре выше.
- После значимого изменения — обнови `PROJECT-BRIEF.md` и (если есть) запись в базе знаний.
- Общение с владельцем — на русском, простыми словами.

## Запуск
{{RUN_COMMAND}}
